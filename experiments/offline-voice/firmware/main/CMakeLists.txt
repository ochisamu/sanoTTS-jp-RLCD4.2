idf_component_register(SRCS "mic_main.c" "tts_bridge.c" "echo_dsp.c" "rlcd42_board.c" "rlcd42_display.c" "rlcd42_battery.c" "../../runtime/ctc_runtime.c" "../../runtime/phonemes.c" "../../runtime/speech_trim.c" "lm_bridge.c" "../../tiny_lm/klm.c"
    INCLUDE_DIRS "." "../../runtime" "../../tiny_lm" REQUIRES driver esp_adc esp_psram esp_timer esp_codec_dev heap esp_partition saanotts_core)
target_compile_options(${COMPONENT_LIB} PRIVATE -Wall -Wextra -Werror=return-type)
set(VOICE_FONT_DIR "${CMAKE_CURRENT_LIST_DIR}/../../.cache/font")
if(NOT EXISTS "${VOICE_FONT_DIR}/shinonome16.h")
    message(FATAL_ERROR "Run: python3 tools/prepare_font.py --download (from repository root)")
endif()
target_sources(${COMPONENT_LIB} PRIVATE "../../runtime/voice_ui.c")
target_include_directories(${COMPONENT_LIB} PRIVATE "${VOICE_FONT_DIR}")
set(REPLY_ASSET_DIR "${CMAKE_CURRENT_LIST_DIR}/../../.cache/reply-assets")
if(NOT EXISTS "${REPLY_ASSET_DIR}/reply_assets.h")
    message(FATAL_ERROR "Run: python tools/prepare_reply_assets.py with the documented training dependencies")
endif()
target_sources(${COMPONENT_LIB} PRIVATE "../../runtime/reply_support.c")
target_include_directories(${COMPONENT_LIB} PRIVATE "${REPLY_ASSET_DIR}")
option(KLM_BPE_RUNTIME "Build the experimental BPE dialogue runtime" OFF)
if(KLM_BPE_RUNTIME)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE KLM_BPE=1 KLM_DIM=256 KLM_HEADS=8 KLM_FF=768)
endif()
